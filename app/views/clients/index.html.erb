<h1>Clients</h1>
<%= link_to "New Post", new_client_path %>
  <ul>
    <% @clients.each do |client| %>
      <li><%= client.name %><br>
        <%= client.tag %>
        <%= image_tag client.image.url(:medium)%>
      </li>
    <% end %>
  </ul>
